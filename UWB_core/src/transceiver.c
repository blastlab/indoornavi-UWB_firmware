#include "transceiver.h"

int transceiver_send(const void *buf, unsigned int len)
{
    TRANSCEIVER_ASSERT(0);
    return 0;
}

int transceiver_estimate_tx_time_us(unsigned int len)
{
    TRANSCEIVER_ASSERT(0);
    return 0;
}

uint64_t transceiver_get_sync_time()
{
    TRANSCEIVER_ASSERT(0);
    return 0;
}